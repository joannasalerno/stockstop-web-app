import LoginForm from "../components/LoginForm";

export default function Login() {
    return (
        <div className="container">
            <h2 className="PageHeader">Login to your account</h2>
            <LoginForm />
        </div>
    );
}